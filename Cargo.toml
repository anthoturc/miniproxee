[package]
name = "miniproxee"
version = "0.1.0"
edition = "2024"

[dependencies]
bytes = "1.12.0"
tower-service = "0.3.3"
tokio = { version = "1", features = ["rt", "net", "macros", "time", "io-util", "sync"] }

[dev-dependencies]
rstest = "0.26.1"
tokio-test = "0.4.5"
rand = "0.10.1"
